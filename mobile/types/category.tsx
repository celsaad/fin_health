import { SFSymbol } from "expo-symbols";

export type Category = {
  id: string;
  name: string;
  icon?: SFSymbol;
};

export type Subcategory = {
  id: string;
  name: string;
  icon?: SFSymbol;
};
